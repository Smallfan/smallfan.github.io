name: Pages

on:
  push:
    branches:
      - master # default branch

jobs:
  build:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 300
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # If your repository depends on submodule, please see: https://github.com/actions/checkout
          submodules: recursive
      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          # Examples: 20, 18.19, >=16.20.2, lts/Iron, lts/Hydrogen, *, latest, current, node
          # Ref: https://github.com/actions/setup-node#supported-version-syntax
          node-version: "22"
      - name: Cache NPM dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.OS }}-npm-cache
          restore-keys: |
            ${{ runner.OS }}-npm-cache
      - name: Cache AI translations
        uses: actions/cache@v4
        with:
          path: .cache/oyster-translations
          key: ${{ runner.OS }}-oyster-translations-${{ hashFiles('source/**/*.md', 'tools/translate-site.mjs', 'translation.config.json') }}
          restore-keys: |
            ${{ runner.OS }}-oyster-translations-
      - name: Cache Qwen model on the GitHub runner
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/oyster-ollama-models
          key: ${{ runner.OS }}-${{ runner.arch }}-ollama-qwen3-8b-v1
      - name: Install Dependencies
        run: npm install
      - name: Install Ollama on the GitHub runner
        run: curl -fsSL https://ollama.com/download/ollama-linux-amd64.tar.zst | sudo tar -x -C /usr
      - name: Build the translated site
        env:
          OLLAMA_MODELS: ${{ runner.temp }}/oyster-ollama-models
          OYSTER_TRANSLATION_PROVIDER: ollama
          OYSTER_TRANSLATION_REQUIRED: "1"
        run: |
          ollama serve > "$RUNNER_TEMP/oyster-ollama.log" 2>&1 &
          ollama_pid=$!
          trap 'kill "$ollama_pid" 2>/dev/null || true' EXIT
          for attempt in {1..30}; do
            if curl -fsS http://127.0.0.1:11434/api/tags >/dev/null; then
              break
            fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:11434/api/tags >/dev/null || {
            tail -100 "$RUNNER_TEMP/oyster-ollama.log"
            exit 1
          }
          ollama pull qwen3:8b
          npm run build
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public
  deploy:
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
