<%
  var archiveTitle = 'Archives';
  if (page.year) archiveTitle += ': ' + page.year;
  if (page.month) archiveTitle += ' / ' + page.month;
%>
<%- partial('_partial/archive-list', { title: archiveTitle }) %>
